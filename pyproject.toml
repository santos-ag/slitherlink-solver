[project]
name = "slitherlink-solver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kayque Pereira", email = "114672278+kaypes@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "networkx>=3.6.1",
    "python-sat>=1.9.dev15",
]

[project.scripts]
slitherlink-solver = "slitherlink_solver:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
